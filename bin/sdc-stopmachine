#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
require 'cli_helper'

begin
  client.machines(ARGV[0]).stop
rescue => e
  puts e.inspect
else
  puts "Success"
end